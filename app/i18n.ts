export type Language = "zh" | "en";

export const UI_TEXT = {
  zh: {
    subtitle: "SDE onsite practice lab",
    solved: "已完成",
    set: "PRACTICE SET · 01",
    headline1: "写真实的代码。",
    headline2: "不刷换皮题。",
    intro: "14 个限时场景，覆盖解析、日期、API、状态、可靠性、流式处理与可测试设计。",
    sourceTitle: "题型依据",
    sourceBody: "综合公开面经中反复出现的解析、日期、API、限流、缓存、重试与流式处理。",
    suggested: "建议",
    requirements: "需求",
    publicTests: "公开测试 · 可展开查看",
    hint: "提示",
    reset: "重置为初始代码",
    restorePassed: "恢复通过版本",
    noPassedVersion: "这道题还没有保存的通过版本",
    run: "▶ Run tests",
    running: "加载 / 运行",
    output: "TEST OUTPUT",
    passed: "ALL PASSED",
    needsWork: "NEEDS WORK",
    idle: "运行代码后，这里会显示公开与隐藏测试结果。",
    loading: "首次加载 Python 约需几秒…",
    timeout: "运行超过 8 秒，已停止。检查是否有死循环。",
    engineError: "Python 引擎加载失败，请检查网络后重试。",
    editorLabel: "Python 代码编辑器",
    navLabel: "练习题列表",
    switchLabel: "Switch to English",
  },
  en: {
    subtitle: "SDE onsite practice lab",
    solved: "solved",
    set: "PRACTICE SET · 01",
    headline1: "Write real code.",
    headline2: "Skip the puzzle reskins.",
    intro: "14 timed scenarios covering parsing, dates, APIs, state, reliability, streaming, and testable design.",
    sourceTitle: "WHY THESE PROBLEMS",
    sourceBody: "Based on recurring patterns in public interview reports: parsing, dates, APIs, rate limiting, caching, retries, and streams.",
    suggested: "Suggested",
    requirements: "REQUIREMENTS",
    publicTests: "PUBLIC TESTS · EXPAND TO VIEW",
    hint: "Hint",
    reset: "Reset starter",
    restorePassed: "Restore passed",
    noPassedVersion: "No passing solution has been saved for this problem",
    run: "▶ Run tests",
    running: "Loading / running",
    output: "TEST OUTPUT",
    passed: "ALL PASSED",
    needsWork: "NEEDS WORK",
    idle: "Run your code to see public and hidden test results.",
    loading: "The first Python load may take a few seconds…",
    timeout: "Execution exceeded 8 seconds and was stopped. Check for an infinite loop.",
    engineError: "The Python runtime failed to load. Check your connection and try again.",
    editorLabel: "Python code editor",
    navLabel: "Practice problem list",
    switchLabel: "切换到中文",
  },
};

export const LEVEL_TEXT = {
  zh: { "热身": "热身", "核心": "核心", "进阶": "进阶" },
  en: { "热身": "Warm-up", "核心": "Core", "进阶": "Advanced" },
};

type EnglishProblem = {
  title: string;
  prompt: string;
  requirements: string[];
  hints: string[];
  publicLabels: string[];
  hiddenLabels: string[];
};

export const ENGLISH_PROBLEMS: Record<string, EnglishProblem> = {
  logs: {
    title: "Production Log Parser",
    prompt: "A monitoring service receives access-log lines. Implement summarize_logs: skip malformed lines, count HTTP status codes, and return the three slowest valid requests.",
    requirements: [
      "Line format: timestamp method path status latency_ms. The final field is request latency in milliseconds.",
      "status must be an integer from 100 through 599; latency_ms must be non-negative.",
      "slowest contains at most three records with timestamp, method, path, status, and latency_ms; sort by descending latency and preserve input order on ties.",
      "Return {by_status: dict[int,int], slowest: list[dict], malformed: int}.",
    ],
    hints: ["Start with a small step that parses one line.", "Python sorting is stable; sort directly with reverse=True to preserve tie order."],
    publicLabels: ["Basic aggregation", "Malformed input"],
    hiddenLabels: ["Stable ordering and empty input"],
  },
  rate: {
    title: "Sliding-Window Rate Limiter",
    prompt: "Implement an in-process rate limiter. Each user has an independent quota: at most limit allowed requests in any window-second interval. Timestamps are non-decreasing.",
    requirements: [
      "Constructor: RateLimiter(limit, window).",
      "allow(user_id, timestamp) returns bool.",
      "The left boundary is exclusive: an old request expires when timestamp - old >= window.",
      "Expired request data must not grow without bound.",
    ],
    hints: ["Maintain one deque per user.", "Remove expired timestamps from the left before checking the length."],
    publicLabels: ["Window boundary", "User isolation"],
    hiddenLabels: ["Rejected calls do not consume quota"],
  },
  cache: {
    title: "TTL-Aware LRU Cache",
    prompt: "Implement a fixed-capacity LRU cache with expiration. The goal is clear semantics and consistent state, not memorizing OrderedDict.",
    requirements: [
      "get(key, now) returns the value and marks the key most recently used; missing or expired keys return None.",
      "put(key, value, now, ttl) inserts or updates an entry; expiry is now + ttl.",
      "When capacity is exceeded, evict the least recently used stored entry.",
      "A cache with capacity <= 0 stores nothing; now >= expiry means expired.",
    ],
    hints: ["OrderedDict.move_to_end and popitem(last=False) express LRU behavior clearly.", "A hash map plus a doubly linked list is also a valid O(1) design."],
    publicLabels: ["LRU eviction", "TTL boundary"],
    hiddenLabels: ["Update and zero capacity"],
  },
  retry: {
    title: "Testable Retry Helper",
    prompt: "Implement retry_call. Real sleep and randomness make tests slow, so the sleeping function is injected.",
    requirements: [
      "fn is a zero-argument callable; return immediately when fn() succeeds.",
      "Catch only the exception types listed in retry_on.",
      "Make at most attempts calls; attempts < 1 must raise ValueError (the message is not constrained).",
      "After retryable failure i, sleep for base_delay * 2**i; do not sleep after the final failure.",
    ],
    hints: ["The first failure sleeps base_delay; the second sleeps 2 * base_delay.", "A bare raise inside except rethrows the original exception."],
    publicLabels: ["Failure, then success", "Do not retry other exceptions"],
    hiddenLabels: ["Final exception and argument validation"],
  },
  merge: {
    title: "Merge Paginated User Data",
    prompt: "API pages may repeat users and omit fields. Merge and normalize records for downstream processing.",
    requirements: [
      "Deduplicate by id; later non-None fields overwrite earlier values.",
      "Ignore records without id.",
      "Every output record has exactly id, name, email, and active.",
      "active defaults to True; sort by the string form of id.",
    ],
    hints: ["Build a mapping from id to the accumulated record.", "Use value is not None rather than truthiness, so False remains meaningful."],
    publicLabels: ["Deduplicate and overwrite", "Ignore malformed records"],
    hiddenLabels: ["None does not overwrite and string ordering"],
  },
  scheduler: {
    title: "Dependency Task Scheduler",
    prompt: "A build system receives task-to-prerequisites configuration. Return execution batches; tasks in one batch can run concurrently. Raise ValueError for cycles or missing dependencies.",
    requirements: [
      "Sort every batch by task name for deterministic output.",
      "A new batch may depend only on tasks completed in earlier batches.",
      "A referenced but undefined prerequisite is invalid configuration.",
      "Empty input returns an empty list.",
    ],
    hints: ["This is a batched form of Kahn's topological sort.", "In each round, collect every remaining task whose prerequisites are complete."],
    publicLabels: ["Concurrent batches", "Cycle detection"],
    hiddenLabels: ["Missing dependency"],
  },
  meetings: {
    title: "Top Meeting Participants",
    prompt: "Aggregate valid meeting duration for every participant and return the top K users. Each participant receives the full duration of a meeting.",
    requirements: [
      "Fields: meeting_id, participants, started_at, ended_at. Ignore records missing any required field.",
      "Times use ISO 8601 with Z or an explicit offset; end must not precede start.",
      "Process only the first occurrence of a meeting_id; count duplicate participants within one meeting once.",
      "Return [{user_id, total_minutes}], sorted by descending duration then ascending str(user_id), limited to top_k.",
    ],
    hints: ["Replace a trailing Z with +00:00, then use datetime.fromisoformat.", "Use a meeting-id set and set(participants)."],
    publicLabels: ["Aggregation and ordering", "Duplicates and invalid records"],
    hiddenLabels: ["Top K, ties, and empty input"],
  },
  recent: {
    title: "Unprocessed Records from the Last 7 Days",
    prompt: "Find records created within the last days days that have not yet been processed. Compare absolute instants relative to the provided now.",
    requirements: [
      "Fields: id, created_at, processed_at. Ignore missing or invalid created_at.",
      "Missing processed_at or processed_at == None means unprocessed; any other value means processed.",
      "Use the inclusive interval [now - days, now]; exclude future records.",
      "Return shallow copies ordered by actual creation time; empty input returns [].",
    ],
    hints: ["Parse now once, then compute lower = now_dt - timedelta(days=days).", "Do not compare ISO strings directly."],
    publicLabels: ["Window and time zones", "Inclusive left boundary"],
    hiddenLabels: ["Invalid dates and empty input"],
  },
  pagination: {
    title: "Safe Paginated API Fetch",
    prompt: "Implement an HTTP-library-independent paginator. fetch_page(cursor) returns one JSON page; the first cursor is None.",
    requirements: [
      "Page shape: {items: list, next_cursor: str | None}. Treat missing or None items as an empty list.",
      "Deduplicate by item id, keeping the first occurrence; ignore items without id.",
      "Stop at next_cursor == None; raise ValueError('cursor cycle') if a cursor repeats.",
      "Return unique items in first-seen order and never assume a fixed page count.",
    ],
    hints: ["Maintain separate seen_ids and seen_cursors sets.", "Call fetch_page(None) first, process the page, then advance the cursor."],
    publicLabels: ["Pagination and deduplication", "Missing items"],
    hiddenLabels: ["Cursor cycle"],
  },
  http_retry: {
    title: "API 429 and Retry Policy",
    prompt: "Implement testable request retries. request_fn() returns a simulated response dictionary or raises TimeoutError; no third-party HTTP package is required.",
    requirements: [
      "Response shape: {status: int, json: any, headers?: dict}. Return json immediately for status 200–299.",
      "Retry 429; use float(Retry-After) when supplied, otherwise exponential backoff.",
      "Retry status 500–599 and TimeoutError. Other statuses immediately raise ValueError('non-retryable status: X').",
      "attempts < 1 raises ValueError. On final failure, rethrow TimeoutError or raise ValueError('request failed: X').",
    ],
    hints: ["Treat each loop iteration as one attempt; never sleep after the last.", "Catch TimeoutError, but inspect HTTP status in the returned dictionary."],
    publicLabels: ["429, then success", "5xx exponential backoff"],
    hiddenLabels: ["Arguments, 4xx, and final failure"],
  },
  webhooks: {
    title: "Webhook Event Deduplication",
    prompt: "A webhook may deliver the same event_id repeatedly. Keep the version with the latest created_at and produce a deterministic replay sequence.",
    requirements: [
      "Fields: event_id, created_at, payload. Ignore missing IDs, missing dates, and invalid dates.",
      "For one event_id, keep the later actual instant; on equal instants keep the later input record.",
      "Return shallow copies sorted by actual created_at ascending.",
      "Support Z and explicit offsets; empty input returns [].",
    ],
    hints: ["Map event_id to (parsed_datetime, record).", "Using >= allows a later input record to win when instants are equal."],
    publicLabels: ["Keep the latest version", "Ignore invalid events"],
    hiddenLabels: ["Later input wins on equal time"],
  },
  profiles: {
    title: "Join Two User APIs",
    prompt: "Merge account and meeting profiles by normalized email. Email comparison ignores case and surrounding whitespace; non-None account fields take priority.",
    requirements: [
      "Possible fields: email, name, company, meeting_count. Ignore missing, None, or blank email.",
      "Output exactly email, name, company, meeting_count; preserve the first-seen original email spelling.",
      "Merge meeting_users first, then let non-None account fields overwrite; later non-None values win within one source.",
      "Keep unmatched users, default missing fields to None, and sort by normalized email.",
    ],
    hints: ["Normalize with str(email).strip().lower().", "Merge meeting users first, then account users, and overwrite only with non-None values."],
    publicLabels: ["Cross-API merge", "Ignore blank email"],
    hiddenLabels: ["None does not overwrite"],
  },
  images: {
    title: "Image Size Statistics",
    prompt: "Each page may contain nested image records. Deduplicate by image id and compute statistics over valid image sizes.",
    requirements: [
      "A page may contain {title, images: list[dict]}; treat missing or None images as [].",
      "A valid image has id and a non-negative int/float size_bytes; bool does not count as numeric.",
      "Count only the first valid record for one image id.",
      "Return {count, total_bytes, average_bytes}; average_bytes is None when no valid images exist.",
    ],
    hints: ["isinstance(True, int) is True, so exclude bool explicitly.", "Add an id to seen only after every validation passes."],
    publicLabels: ["Nested data, dedupe, and average", "Empty result"],
    hiddenLabels: ["Invalid types"],
  },
  stream: {
    title: "Streaming Status Aggregation",
    prompt: "Process records from a file or generator: count statuses while retaining only the five most recent error records.",
    requirements: [
      "Input is Iterable[dict] and may be consumed only once; do not call len, index it, or convert it to list.",
      "Records missing status increment malformed; count all other status values.",
      "For status == 'error', retain a shallow copy; keep only the final five in input order.",
      "Return {by_status, recent_errors, malformed}; an empty stream returns empty statistics.",
    ],
    hints: ["deque(maxlen=5) automatically discards the oldest entry.", "Convert the deque to list only for the final result; never pre-scan records."],
    publicLabels: ["Single-pass generator aggregation", "Keep only the final five"],
    hiddenLabels: ["Reject repeated iteration"],
  },
};
